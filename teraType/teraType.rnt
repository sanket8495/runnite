foreach my $f (@tt_list) {
   `turi-compressor $RSC_DIR/$f -o $RSC_DIR/min/$dest`;
}

tt_list = {
  [postmodern],
  [turi],
  [tzdata],
  [abys],
}

@type check :: boolean()
:clientIP.start_instance((*ip-here*).tls_config(basic))
:tls_options{
   |> IGNITE_STATUS <|
}
